name: pub_package_reader
description: Scans the package archive, extracts its main content and checks for errors.
publish_to: none

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  logging: ^0.11.3
  meta: ^1.3.0
  pub_semver: ^1.4.4
  pubspec_parse: ^0.1.4
  yaml: ^2.0.0
  tar: 0.3.2

dev_dependencies:
  http: ^0.13.0
  test: ^1.16.5
