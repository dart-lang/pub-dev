name: signature_verifier
description: A tool/library to verify RSA SHA-256 signatures.
version: 0.1.0

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  webcrypto: ^0.6.0
  pem: ^2.0.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
