name: web_app
publish_to: none

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  mdc_web: ^0.4.1+1
  api_builder:
    path: ../api_builder
  client_data:
    path: ../client_data
  http: ^0.12.0
  js: ^0.6.1
  markdown: ^2.1.0
  intl: ^0.16.0

dev_dependencies:
  test: ^1.0.0
