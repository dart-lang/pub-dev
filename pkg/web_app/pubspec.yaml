name: web_app
publish_to: none

environment:
  sdk: '^3.4.0'

dependencies:
  _pub_shared:
    path: ../_pub_shared
  api_builder:
    path: ../api_builder
  http: ^1.0.0
  markdown: ^7.2.0
  mdc_web: ^0.6.0
  meta: ^1.3.0
  web: ^1.0.0

dev_dependencies:
  test: ^1.16.5
